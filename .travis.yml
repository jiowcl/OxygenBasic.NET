# Native oxygen.dll / oxygen64.dll require Windows.
# CI is hosted on GitHub Actions (windows-latest, x86 + x64). See .github/workflows/dotnetcore.yml
language: csharp
dist: jammy
mono: none

dotnet:
  - 10.0

script:
  - echo "Travis CI is Linux-only; OxygenBasic.NET native tests run on GitHub Actions (Windows x86/x64)."
  - exit 0
